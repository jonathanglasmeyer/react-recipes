'use strict';
// require('');

module.exports = React.createClass({
    render() {
        return (
          <input type="submit" value="delete all" className="button grey-button" />
        );
    }
});

